import 'package:flutter/material.dart';

class FamiliesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child:  Text('Pantalla de Families'),
    );
  }
}